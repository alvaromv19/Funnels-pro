<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EVO LAUNCH | High Ticket Scaling</title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&family=Space+Grotesk:wght@500;700&display=swap" rel="stylesheet">
    
    <script src="https://unpkg.com/@phosphor-icons/web"></script>

    <style>
        /* =========================================
           1. VARIABLES & RESET
           ========================================= */
        :root {
            --bg-main: #0B0C10;
            --bg-card: #1F2833;
            --accent-cyan: #66FCF1;
            --accent-cyan-dim: #45A29E;
            --accent-purple: #C542CB;
            --text-main: #FFFFFF;
            --text-muted: #C5C6C7;
            
            --font-heading: 'Space Grotesk', sans-serif;
            --font-body: 'Inter', sans-serif;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background-color: var(--bg-main);
            color: var(--text-main);
            font-family: var(--font-body);
            line-height: 1.6;
            overflow-x: hidden;
            font-size: 18px; /* Base size requested */
        }

        h1, h2, h3, h4 {
            font-family: var(--font-heading);
            font-weight: 700;
            line-height: 1.2;
        }

        p {
            color: var(--text-muted);
            margin-bottom: 1.5rem;
        }

        /* =========================================
           2. UTILITIES & UI KIT
           ========================================= */
        .container {
            max-width: 1100px;
            margin: 0 auto;
            padding: 0 20px;
        }

        .text-gradient {
            background: linear-gradient(90deg, var(--accent-cyan), var(--accent-purple));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .text-center { text-align: center; }
        .mt-2 { margin-top: 2rem; }
        .mb-2 { margin-bottom: 2rem; }

        /* --- BOTONES NEÓN --- */
        .btn-neon {
            display: inline-block;
            background-color: var(--accent-cyan);
            color: #000;
            font-family: var(--font-heading);
            font-weight: 700;
            padding: 16px 32px;
            border-radius: 6px;
            text-decoration: none;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
            box-shadow: 0 0 10px rgba(102, 252, 241, 0.3);
            animation: pulse 5s infinite;
        }

        .btn-neon:hover {
            background-color: #fff;
            color: var(--accent-cyan);
            box-shadow: 0 0 20px var(--accent-cyan), 0 0 40px var(--accent-cyan);
            transform: translateY(-2px);
        }

        .btn-small-text {
            display: block;
            font-size: 0.8rem;
            margin-top: 8px;
            font-weight: 400;
            opacity: 0.8;
            color: var(--text-muted);
        }

        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.02); box-shadow: 0 0 15px rgba(102, 252, 241, 0.5); }
            100% { transform: scale(1); }
        }

        /* --- GLASSMORPHISM CARDS --- */
        .glass-card {
            background: rgba(31, 40, 51, 0.6);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 12px;
            padding: 2rem;
            transition: all 0.3s ease;
        }

        .glass-card:hover {
            border-color: var(--accent-purple);
            box-shadow: 0 0 20px rgba(197, 66, 203, 0.2);
            transform: translateY(-5px);
        }

        /* --- SCROLL REVEAL ANIMATION --- */
        .reveal {
            opacity: 0;
            transform: translateY(30px);
            transition: all 0.8s ease-out;
        }
        .reveal.active {
            opacity: 1;
            transform: translateY(0);
        }

        /* =========================================
           3. SECTIONS
           ========================================= */
        
        /* HERO SECTION */
        .hero {
            min-height: 90vh;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            background: radial-gradient(circle at top right, rgba(197, 66, 203, 0.2), transparent 40%);
            padding: 100px 0;
        }

        .hero-content {
            max-width: 900px;
            text-align: center;
            z-index: 2;
        }

        .pre-headline {
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 2px;
            color: var(--accent-purple);
            margin-bottom: 1rem;
            font-weight: 700;
        }

        .hero h1 {
            font-size: 3.5rem;
            margin-bottom: 1.5rem;
            color: var(--text-main);
        }

        .hero-sub {
            font-size: 1.2rem;
            color: var(--text-muted);
            max-width: 800px;
            margin: 0 auto 3rem auto;
        }

        /* PROBLEM SECTION (Console Style) */
        .problem-section {
            padding: 80px 0;
            background-color: var(--bg-main);
        }

        .console-container {
            max-width: 700px;
            margin: 0 auto;
            border-left: 2px solid var(--accent-purple);
            padding-left: 30px;
        }

        .console-container h2 {
            font-size: 2rem;
            margin-bottom: 2rem;
        }

        /* MECHANISM DIAGRAM (CSS Only) */
        .mechanism-section {
            padding: 100px 0;
            background-color: #0e0f14;
            text-align: center;
        }

        .diagram-container {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 20px;
            margin-top: 50px;
            flex-wrap: wrap;
        }

        .diagram-node {
            border: 1px solid var(--accent-cyan);
            padding: 20px;
            border-radius: 8px;
            min-width: 150px;
            position: relative;
            background: rgba(11, 12, 16, 0.8);
        }
        
        .node-traffic { border-color: #fff; }
        .node-high { border-color: var(--accent-cyan); box-shadow: 0 0 10px rgba(102, 252, 241, 0.2); }
        .node-down { border-color: var(--accent-purple); }

        .arrow {
            font-size: 2rem;
            color: var(--accent-cyan);
        }

        .diagram-label {
            display: block;
            font-weight: bold;
            font-family: var(--font-heading);
            margin-top: 10px;
            text-transform: uppercase;
        }

        /* DELIVERABLES (Grid) */
        .deliverables-section {
            padding: 100px 0;
        }

        .grid-2x2 {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            margin-top: 50px;
        }

        .deliverable-icon {
            font-size: 2rem;
            color: var(--accent-purple);
            margin-bottom: 1rem;
        }

        /* FOOTER / CTA */
        .footer-cta {
            padding: 100px 0;
            background-color: var(--bg-card);
            text-align: center;
        }

        .checklist {
            list-style: none;
            margin: 30px auto;
            max-width: 500px;
            text-align: left;
        }

        .checklist li {
            margin-bottom: 15px;
            display: flex;
            align-items: center;
        }

        .checklist li i {
            color: var(--accent-cyan);
            margin-right: 15px;
            font-size: 1.2rem;
        }

        /* STICKY MOBILE CTA */
        .sticky-mobile-cta {
            position: fixed;
            bottom: 0;
            left: 0;
            width: 100%;
            background: rgba(11, 12, 16, 0.95);
            padding: 15px;
            border-top: 1px solid var(--accent-cyan);
            z-index: 100;
            display: none; /* JS will toggle this */
            text-align: center;
            backdrop-filter: blur(5px);
        }

        .sticky-mobile-cta .btn-neon {
            width: 100%;
            padding: 12px;
            font-size: 0.9rem;
        }

        /* RESPONSIVE */
        @media (max-width: 768px) {
            .hero h1 { font-size: 2.2rem; }
            .console-container { padding-left: 15px; border-left-width: 1px; }
            .diagram-container { flex-direction: column; }
            .arrow { transform: rotate(90deg); }
        }
    </style>
</head>
<body>

    <header class="hero">
        <div class="container hero-content reveal">
            <p class="pre-headline">ATENCIÓN: PARA DUEÑOS DE MARCAS, INFOPRODUCTORES Y CREADORES CON TRAYECTORIA</p>
            <h1>Deja de Construir "Escaleras de Valor". <br>Están <span class="text-gradient">Matando Tu Margen.</span></h1>
            <p class="hero-sub">Descubre el sistema "Directo-a-la-Yugular" para vender High Ticket ($1k-$5k) a tráfico frío desde el día 1, utilizando un "Mecanismo de Seguridad" que hace que tu publicidad sea matemáticamente gratis.</p>
            
            <a href="#audit" class="btn-neon">
                QUIERO AGENDAR MI AUDITORÍA DE ESCALAMIENTO
            </a>
            <span class="btn-small-text">Plazas limitadas para este mes</span>
        </div>
    </header>

    <section class="problem-section">
        <div class="container console-container reveal">
            <h2>Estimado dueño de negocio,</h2>
            <p>Si tienes una marca con seguidores, una lista de clientes o un producto validado, pero al final del mes miras tus cuentas y te preguntas: <strong>“¿Dónde está el beneficio real de todo este esfuerzo?”</strong>, lee esto con atención.</p>
            
            <p>Probablemente te han dicho que para vender algo de $1,000, $2,000 o $5,000 dólares a gente que no te conoce, primero tienes que "calentarlos".</p>
            
            <div style="background: rgba(255,255,255,0.05); padding: 20px; border-radius: 8px; margin: 20px 0;">
                <p style="margin-bottom: 0.5rem; color: #fff;">❌ La vieja "Escalera de Valor":</p>
                <ul style="padding-left: 20px; color: var(--text-muted);">
                    <li>Regalar un Lead Magnet.</li>
                    <li>Vender un producto barato de $27.</li>
                    <li>Vender un curso medio.</li>
                    <li>Y meses después... ofrecer tu servicio Premium.</li>
                </ul>
            </div>

            <p><strong>Te han mentido.</strong> O al menos, te han dado un consejo obsoleto.</p>
            <p>La "Escalera de Valor" es la forma más lenta, dolorosa y costosa de escalar un negocio hoy en día. Mientras tú pasas meses "nutriendo" a un prospecto con contenido gratuito, tu competencia (que es más agresiva) ya le vendió la solución completa.</p>
        </div>
    </section>

    <section style="padding: 80px 0; background-color: #15191e;">
        <div class="container text-center reveal">
            <h2 class="text-gradient" style="font-size: 2.5rem; margin-bottom: 2rem;">La Verdad sobre tu Mercado</h2>
            <div style="max-width: 800px; margin: 0 auto; text-align: left;">
                <p>En este preciso momento, el <strong>3% de tu mercado</strong> está en lo que llamamos "Modo de Compra Inmediata". Tienen un dolor agudo, tienen el dinero y quieren la solución YA. No quieren leer tu ebook gratuito. Quieren pagarte para que les resuelvas el problema.</p>
                <p>Si los obligas a pasar por un embudo lento, los pierdes.</p>
                <p>En <strong>EVO LAUNCH</strong>, hemos desarrollado una metodología diferente. Nosotros no buscamos likes. No buscamos "engagement". Nosotros conectamos tu oferta más cara con tráfico frío de manera rentable.</p>
            </div>
        </div>
    </section>

    <section class="mechanism-section">
        <div class="container reveal">
            <h2>Cómo Funciona: <span class="text-gradient">El Mecanismo de Seguridad</span></h2>
            <p style="max-width: 600px; margin: 20px auto;">Quizás estés pensando: "¿Es arriesgado ir a tráfico frío?". Nuestro sistema integra un seguro financiero.</p>

            <div class="diagram-container">
                <div class="diagram-node node-traffic">
                    <i class="ph ph-users-three" style="font-size: 2rem;"></i>
                    <span class="diagram-label">Tráfico Frío</span>
                    <small>Ads Directos</small>
                </div>

                <div class="arrow"><i class="ph ph-arrow-right"></i></div>

                <div class="diagram-node node-high">
                    <i class="ph ph-crown" style="font-size: 2rem; color: var(--accent-cyan);"></i>
                    <span class="diagram-label" style="color: var(--accent-cyan);">HIGH TICKET</span>
                    <small>Filtro de Intención</small>
                </div>

                <div class="arrow"><i class="ph ph-arrow-elbow-right-down"></i></div>

                <div class="diagram-node node-down">
                    <i class="ph ph-currency-dollar" style="font-size: 2rem; color: var(--accent-purple);"></i>
                    <span class="diagram-label" style="color: var(--accent-purple);">DOWNSELL</span>
                    <small>Monetización del "NO"</small>
                </div>
            </div>

            <div style="max-width: 700px; margin: 40px auto; text-align: left; background: rgba(255,255,255,0.03); padding: 20px; border-radius: 10px;">
                <p><strong>El resultado matemático:</strong> Las ventas de este producto de "consuelo" (Downsell) cubren el coste de tus anuncios. Tus campañas se pagan con los "No", y te quedas con el 100% de beneficio limpio de los "Sí" del High Ticket.</p>
            </div>
        </div>
    </section>

    <section class="deliverables-section">
        <div class="container reveal">
            <h2 class="text-center">No somos una agencia creativa.<br>Somos Socios de Crecimiento.</h2>
            
            <div class="grid-2x2">
                <div class="glass-card">
                    <div class="deliverable-icon"><i class="ph ph-gear"></i></div>
                    <h3>Ingeniería de Oferta</h3>
                    <p style="margin-top: 10px; font-size: 0.95rem;">Transformamos tu servicio en una Oferta Irresistible High Ticket que justifica precios premium.</p>
                </div>
                <div class="glass-card">
                    <div class="deliverable-icon"><i class="ph ph-funnel"></i></div>
                    <h3>Sistema de Doble Embudo</h3>
                    <p style="margin-top: 10px; font-size: 0.95rem;">La tecnología e infraestructura técnica para captar clientes Premium y monetizar a los curiosos.</p>
                </div>
                <div class="glass-card">
                    <div class="deliverable-icon"><i class="ph ph-pen-nib"></i></div>
                    <h3>Copywriting Directo</h3>
                    <p style="margin-top: 10px; font-size: 0.95rem;">Guiones y textos diseñados psicológicamente para vender, no para entretener.</p>
                </div>
                <div class="glass-card">
                    <div class="deliverable-icon"><i class="ph ph-chart-line-up"></i></div>
                    <h3>Dashboard de Inteligencia</h3>
                    <p style="margin-top: 10px; font-size: 0.95rem;">Un panel de control para ver tu retorno de inversión (ROI) en tiempo real sin métricas vanidosas.</p>
                </div>
            </div>
        </div>
    </section>

    <footer class="footer-cta" id="audit">
        <div class="container reveal">
            <h2 style="font-size: 3rem; margin-bottom: 2rem;">Esto NO es para todo el mundo.</h2>
            <p>Buscamos casos de éxito, no experimentos. Solo podemos ayudarte si:</p>
            
            <ul class="checklist">
                <li><i class="ph ph-check-circle"></i> Ya tienes una marca o trayectoria validada.</li>
                <li><i class="ph ph-check-circle"></i> Tienes capacidad de inversión para escalar.</li>
                <li><i class="ph ph-check-circle"></i> Estás dispuesto a dejar de jugar a "vender barato".</li>
            </ul>

            <p style="margin-bottom: 3rem;">Si vemos el potencial de agregar <strong>$30k extra</strong> a tu facturación en 90 días, te explicaremos cómo trabajar con nosotros.</p>
            
            <a href="#" class="btn-neon">
                SÍ, QUIERO AGENDAR MI AUDITORÍA GRATUITA
            </a>
            <p class="btn-small-text">El mercado premia a los que actúan rápido.</p>
        </div>
    </footer>

    <div class="sticky-mobile-cta" id="mobileSticky">
        <a href="#audit" class="btn-neon">AGENDAR AUDITORÍA</a>
    </div>

    <script>
        // 1. Scroll Reveal Logic
        const revealElements = document.querySelectorAll('.reveal');

        const revealOnScroll = () => {
            const windowHeight = window.innerHeight;
            const elementVisible = 150;

            revealElements.forEach((reveal) => {
                const elementTop = reveal.getBoundingClientRect().top;
                if (elementTop < windowHeight - elementVisible) {
                    reveal.classList.add('active');
                }
            });
        };

        window.addEventListener('scroll', revealOnScroll);
        // Trigger once on load
        revealOnScroll();

        // 2. Sticky Button Logic (Mobile Only)
        const mobileSticky = document.getElementById('mobileSticky');
        
        window.addEventListener('scroll', () => {
            // Solo activar si es pantalla pequeña
            if (window.innerWidth <= 768) {
                const scrollPercent = (window.scrollY / (document.body.scrollHeight - window.innerHeight)) * 100;
                
                if (scrollPercent > 20) { // Aparece después del 20% de scroll
                    mobileSticky.style.display = 'block';
                } else {
                    mobileSticky.style.display = 'none';
                }
            }
        });
    </script>
</body>
</html>
